//Files in this directory are main components for the pages are not small components(ex: button, input, etc) these can be (ex: Hero section, Features ,etc)

import React from 'react'

export default function Exemple() {
  return (
    <div>
      
    </div>
  )
}
